server:
  port: 9095
spring:
  application:
    name: product-service
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/productdb
    username: billing-product
    password: billing123$$
    driver-class-name: com.mysql.cj.jdbc.Driver
eureka:
  instance:
    hostname: 52.66.110.212
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:8761/eureka/
billing:
  app: This is billing App Product Service default